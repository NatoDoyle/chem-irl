# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_KEY=your_supabase_publishable_key

# App Configuration
EXPO_PUBLIC_APP_URL=https://chemirl.app

# Magic Link Authentication Redirect
# Deep link URL for magic link authentication
# Format: <scheme>://auth/callback
# 
# IMPORTANT: Different URLs for different build types:
# - Expo Go: Uses exp://localhost:8081/--/auth/callback (handled automatically by Linking.createURL)
# - Dev/Prod builds: Uses chemirl://auth/callback (matches app.json scheme)
# 
# Defaults to Linking.createURL('/auth/callback') if not set
# For Expo Go, this will be exp://localhost:8081/--/auth/callback
# For dev/prod builds, set explicitly to chemirl://auth/callback
EXPO_PUBLIC_AUTH_REDIRECT_URL=chemirl://auth/callback

# Optional: Error Logging (Sentry)
EXPO_PUBLIC_SENTRY_DSN=your_sentry_dsn_here
EXPO_PUBLIC_ENVIRONMENT=development
